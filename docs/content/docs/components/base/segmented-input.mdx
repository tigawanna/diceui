---
base: base
title: Segmented Input
description: A group of connected input fields that appear as a single segmented visual unit.
preview: false
links:
 api: /docs/components/base/segmented-input#api-reference
---

<ComponentTabs name="segmented-input-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-install
npx shadcn@latest add @diceui/segmented-input
```

</TabsContent>

<TabsContent value="manual">

<Steps className="pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="segmented-input" />

</Steps>

</TabsContent>

</CodeTabs>

## Layout

Import the parts, and compose them together.

```tsx
import * as SegmentedInput from "@/components/ui/segmented-input";

return (
  <SegmentedInput.Root>
    <SegmentedInput.Item />
  </SegmentedInput.Root>
)
```

## Examples

### Form Input

Use segmented inputs for structured form data like phone numbers or addresses.

<ComponentTabs name="segmented-input-form-demo" />

### RGB Color Input

Create color input controls using segmented inputs for RGB values.

<ComponentTabs name="segmented-input-rgb-demo" />

### Vertical Layout

Display segmented inputs in a vertical orientation.

<ComponentTabs name="segmented-input-vertical-demo" />

## API Reference

### SegmentedInput

The main segmented input container.

<AutoTypeTable
  path="./types/base/segmented-input.ts"
  name="SegmentedInputProps"
/>

### SegmentedInputItem

Individual input items within the segmented input.

<AutoTypeTable
  path="./types/base/segmented-input.ts"
  name="SegmentedInputItemProps"
/>

## Accessibility

The SegmentedInput component follows standard web accessibility practices. Users navigate between inputs using Tab and Shift+Tab keys, which is the expected behavior for form controls.

### Keyboard Interactions

<KeyboardShortcutsTable
  data={[
    {
      keys: ["Tab"],
      description: "Moves focus to the next input in the segment.",
    },
    {
      keys: ["Shift", "Tab"],
      description: "Moves focus to the previous input in the segment.",
    },
  ]}
/>

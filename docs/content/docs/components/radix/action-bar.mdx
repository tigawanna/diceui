---
base: radix
title: Action Bar
description: A floating action bar that appears at the bottom or top of the viewport to display contextual actions for selected items.
preview: false
links:
 api: /docs/components/radix/action-bar#api-reference
---

<ComponentTabs name="action-bar-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "@diceui/action-bar"

```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @radix-ui/react-slot @radix-ui/react-direction
     ```
  </Step>
  <Step>
    Copy and paste the portal component into your `components/portal.tsx` file.

    <ComponentSource name="portal" />
  </Step>
  <Step>
    Copy and paste the refs composition utilities into your `lib/compose-refs.ts` file.

    <ComponentSource name="compose-refs" />
  </Step>
  <Step>
    Copy and paste the following hooks into your `hooks` directory.

    <ComponentSource name="use-isomorphic-layout-effect" />
    <ComponentSource name="use-as-ref" />
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="action-bar" />
  </Step>
  <Step>
    Update the import paths to match your project setup.
  </Step>
</Steps>

## Layout

```tsx
import {
  ActionBar,
  ActionBarSelection,
  ActionBarSeparator,
  ActionBarGroup,
  ActionBarItem,
  ActionBarClose,
} from "@/components/ui/action-bar";

return (
  <ActionBar>
    <ActionBarSelection />
    <ActionBarSeparator />
    <ActionBarGroup>
      <ActionBarItem />
      <ActionBarItem />
    </ActionBarGroup>
    <ActionBarClose />
  </ActionBar>
);
```

## Examples

### Position

Use the `side` and `align` props to control where the action bar appears.

<ComponentTabs name="action-bar-position-demo" />

## API Reference

### ActionBar

The root component that controls the visibility and position of the action bar. Has `role="toolbar"` for accessibility.

<AutoTypeTable
  path="./types/docs/action-bar.ts"
  name="ActionBarProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-side]",
      value: ["top", "bottom"],
    },
    {
      title: "[data-align]",
      value: ["start", "center", "end"],
    },
    {
      title: "[data-orientation]",
      value: ["horizontal", "vertical"],
    },
  ]}
/>

### ActionBarSelection

Displays selection information, typically used to show how many items are selected.

<AutoTypeTable
  path="./types/docs/action-bar.ts"
  name="ActionBarSelectionProps"
/>

### ActionBarGroup

A container for action items that implements roving focus management. Items within a group can be navigated using arrow keys, forming a single tab stop. See [Keyboard Interactions](#keyboard-interactions) for full details.

<AutoTypeTable
  path="./types/docs/action-bar.ts"
  name="ActionBarGroupProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-orientation]",
      value: ["horizontal", "vertical"],
    },
  ]}
/>

### ActionBarItem

An interactive button item within the action bar. When used inside a `Group`, participates in roving focus navigation.

<AutoTypeTable
  path="./types/docs/action-bar.ts"
  name="ActionBarItemProps"
/>

### ActionBarClose

A button that closes the action bar by calling the `onOpenChange` callback with `false`. The close button has its own tab stop, separate from the group's roving focus.

<AutoTypeTable
  path="./types/docs/action-bar.ts"
  name="ActionBarCloseProps"
/>

### ActionBarSeparator

A visual separator between action bar items.

<AutoTypeTable
  path="./types/docs/action-bar.ts"
  name="ActionBarSeparatorProps"
/>


## Accessibility

### Keyboard Interactions

The action bar follows the [WAI-ARIA Toolbar pattern](https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/) for keyboard navigation.

<KeyboardShortcutsTable
  data={[
    {
      keys: ["Tab"],
      description: "Moves focus to the next focusable element (Action Group or Close button).",
    },
    {
      keys: ["Shift", "Tab"],
      description: "Moves focus to the previous focusable element.",
    },
    {
      keys: ["Escape"],
      description: "Closes the action bar and calls onOpenChange(false).",
    },
    {
      keys: ["ArrowLeft"],
      description: "Moves focus to the previous item in the group (horizontal orientation).",
    },
    {
      keys: ["ArrowUp"],
      description: "Moves focus to the previous item in the group (vertical orientation).",
    },
    {
      keys: ["ArrowRight"],
      description: "Moves focus to the next item in the group (horizontal orientation).",
    },
    {
      keys: ["ArrowDown"],
      description: "Moves focus to the next item in the group (vertical orientation).",
    },
    {
      keys: ["Home"],
      description: "Moves focus to the first item in the group.",
    },
    {
      keys: ["End"],
      description: "Moves focus to the last item in the group.",
    },
  ]}
/>

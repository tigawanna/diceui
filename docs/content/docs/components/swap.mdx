---
title: Swap
description: A component that swaps between two states with click or hover activation modes.
preview: true
links:
  api: /docs/components/swap#api-reference
---

<ComponentTabs name="swap-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add @diceui/swap
```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @radix-ui/react-slot
     ```
  </Step>
  <Step>
    Copy and paste the following hooks into your `hooks` directory.

    <ComponentSource name="use-as-ref" />
    <ComponentSource name="use-isomorphic-layout-effect" />
    <ComponentSource name="use-lazy-ref" />
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="swap" />
  </Step>
  <Step>
    Update the import paths to match your project setup.
  </Step>
</Steps>

## Layout

Import the parts, and compose them together.

```tsx
import { Swap, SwapOn, SwapOff } from "@/components/ui/swap";

return (
  <Swap>
    <SwapOn />
    <SwapOff />
  </Swap>
)
```

## Examples

### Animations

The swap component supports 4 different animation types: fade, rotate, flip, and scale.

<ComponentTabs name="swap-animations-demo" />

## API Reference

### Swap

The main container component for swap functionality.

<AutoTypeTable
  path="./types/docs/swap.ts"
  name="SwapProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-state]",
      value: ["on", "off"],
    },
    {
      title: "[data-animation]",
      value: ["fade", "rotate", "flip", "scale"],
    },
    {
      title: "[data-disabled]",
      value: "Present when the swap is disabled",
    },
  ]}
/>

### SwapOn

The content shown when the swap is in the swapped state.

<AutoTypeTable
  path="./types/docs/swap.ts"
  name="SwapOnProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-state]",
      value: ["on", "off"],
    },
  ]}
/>

### SwapOff

The content shown when the swap is in the default state.

<AutoTypeTable
  path="./types/docs/swap.ts"
  name="SwapOffProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-state]",
      value: ["on", "off"],
    },
  ]}
/>

## Accessibility

### Keyboard Interactions

<KeyboardShortcutsTable
  data={[
    {
      keys: ["Enter"],
      description: "Toggles the swap state when activation mode is 'click'.",
    },
    {
      keys: ["Space"],
      description: "Toggles the swap state when activation mode is 'click'.",
    },
  ]}
/>

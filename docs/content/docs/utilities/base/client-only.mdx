---
base: base
title: Client Only
description: Renders client-only components with hydration and fallback support.
links:
  api: /docs/utilities/base/client-only#api-reference
---

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-install
npx shadcn@latest add @diceui/client-only
```

</TabsContent>

<TabsContent value="manual">

<Steps className="pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource src="./registry/bases/base/components/client-only.tsx" />

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ClientOnly } from "@/components/client-only"

export default function App() {
  return (
    <ClientOnly fallback={<LoadingSpinner />}>
      <ClientComponent />
    </ClientOnly>
  )
}
```

## API Reference

### ClientOnly

A component that only renders its children on the client side.

<AutoTypeTable
  path="./types/base/utilities.ts"
  name="ClientOnlyProps"
/>
